import { mount } from '@jscutlery/cypress-mount';

describe('greetings', () => {

  it('should say hello', () => {
    // mount(MyGreetingsComponent);
    // cy.get('h1').contains('hello');
  });

});
